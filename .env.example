PORT=4000

# Render Postgres (Internal Database URL)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Frontend domains (comma-separated)
CORS_ORIGINS=https://your-job-by-isroilov.netlify.app,http://localhost:5173

# Optional: JSON file path when DATABASE_URL is not set (local dev)
# DATABASE_FILE=/data/database.json
